abstract class RequestParams {
  static const String sortBy = '_sortBy';
  static const String where = '_where';
  static const String startRow = '_startRow';
  static const String endRow = '_endRow';
  static const String selectedProp = '_selectedProperties';
}
